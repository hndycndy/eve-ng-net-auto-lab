name: Network CI/CD

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: self-hosted
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Ansible
        run: |
          python -m pip install --upgrade pip
          pip install ansible paramiko ansible-pylibssh
          ansible-galaxy collection install -r requirements.yml

      - name: Syntax check
        run: |
          ansible-playbook --syntax-check playbooks/render.yml
          ansible-playbook --syntax-check playbooks/deploy.yml
          ansible-playbook --syntax-check playbooks/verify.yml

      - name: Render configs
        run: |
          ansible-playbook playbooks/render.yml

      - name: Upload rendered configs
        uses: actions/upload-artifact@v4
        with:
          name: rendered-configs
          path: rendered/

  deploy:
    if: github.ref == 'refs/heads/main'
    needs: validate
    runs-on: self-hosted
    environment: lab
    env:
      LAB_ANSIBLE_USER: ${{ secrets.LAB_ANSIBLE_USER }}
      LAB_ANSIBLE_PASSWORD: ${{ secrets.LAB_ANSIBLE_PASSWORD }}
      LAB_ENABLE_PASSWORD: ${{ secrets.LAB_ENABLE_PASSWORD }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install Ansible
        run: |
          python -m pip install --upgrade pip
          pip install ansible paramiko ansible-pylibssh
          ansible-galaxy collection install -r requirements.yml

      - name: Render configs
        run: |
          ansible-playbook playbooks/render.yml

      - name: Validate access to routers
        run: |
          ansible-playbook playbooks/validate.yml

      - name: Deploy configs
        run: |
          ansible-playbook playbooks/deploy.yml

      - name: Verify routing
        run: |
          ansible-playbook playbooks/verify.yml
