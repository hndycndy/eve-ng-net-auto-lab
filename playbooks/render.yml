---
- name: Render intended configs
  hosts: routers
  gather_facts: false
  tasks:
    - name: Render per-router config
      ansible.builtin.template:
        src: ../templates/router_config.j2
        dest: "../rendered/{{ inventory_hostname }}.cfg"
      delegate_to: localhost
