---
- name: Deploy intended config to routers
  hosts: routers
  gather_facts: false
  tasks:
    - name: Push rendered config
      cisco.ios.ios_config:
        src: "../rendered/{{ inventory_hostname }}.cfg"
        save_when: modified
